# Мастер-класс: Инференс ML-моделей (60 мин)

## 1. Введение (7 мин)
- Что такое инференс и почему это важно
- Обзор стека: PyTorch → Triton → vLLM
- Что студенты унесут с собой

## 2. Модель: подготовка и экспорт (13 мин)
- Берём готовую модель (ResNet / BERT-tiny)
- Экспорт в ONNX — показываем код
- Почему формат модели важен для инференса

## 3. Triton Inference Server (28 мин)
- Что такое Triton, зачем нужен (батчинг, параллелизм, метрики)
- Структура `model_repository`, пишем `config.pbtxt`
- Запуск сервера в Docker
- Запрос через `tritonclient`
- Смотрим throughput / latency

## 4. vLLM (8 мин)
- Чем LLM-инференс отличается от классического
- PagedAttention в двух словах
- `vllm serve` + curl-запрос
- Сравнение с наивным HuggingFace inference

## 5. Q&A и итоги (4 мин)
- Куда копать дальше: TensorRT, SGLang, CUDA, Dynamo
- Ссылки на материалы
